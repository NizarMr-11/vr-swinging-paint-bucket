# Diagnostics

Run manifests (`HarmonicRunManifest`), multi-channel logging hub, GPU readback utilities, and pipeline stats overlays.

`HarmonicGoldenFrameTests` captures avg constraint, speed, max Y, and active count for regression.
